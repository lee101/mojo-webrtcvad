[project]
name = "mojo-webrtcvad"
version = "0.1.0"
description = "WebRTC voice activity detection implemented in Mojo"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Lee Penkman", email = "leepenkman@gmail.com" }]
keywords = ["mojo", "webrtc", "vad", "audio"]
dependencies = ["numpy"]

[project.optional-dependencies]
dev = ["pytest", "webrtcvad==2.0.10"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "" = "python" }

[tool.setuptools.packages.find]
where = ["python"]
